# إصلاح الأخطاء المحاسبية + حذف الكود الميت

مرجع التشخيص: `CODE_AUDIT.md`. هذا الملف يوثّق **ما نُفِّذ فعلاً**.

**التحقّق:** كل الاستعلامات المحاسبية الجديدة شُغِّلت على قاعدة SQLite حقيقية بعد تطبيق
الـ SCHEMA و146 migration، بأرقام معروفة سلفاً — **13 تأكيداً ناجحاً، صفر فاشل**.
`tsc` على المشروعين + `electron-vite build` يمرّان.

---

## أولاً: الأخطاء المحاسبية (أُصلحت كلها)

### 1. «الإيرادات» و«صافي الربح» — `stats.ts`
كانا `SUM(amount_in)`، وهو بعد migration توحيد الاتجاه لا يحتوي إلا على **التحصيل**.

الآن الإيرادات تُقرأ من مصدرها الحقيقي — مبيعات الشيفتات:
`pos_sales + (fawry_total_manual أو program_sales)` — بنفس تعريف «اجمالي المبيعات» في ملخّص الشيفت.

واستُبعدت فئتا `income` و`collection` من جانب التكاليف: منصرف فئة «مبيعات» (آجل/فيزا)
أثر جانبي لتوحيد الاتجاه لا مصروف، وكان يُحتسَب تكلفةً فينفخ الخسارة.

| | قبل | بعد |
|---|---|---|
| إيرادات يوليو (POS 1000ج + فوري 200ج) | 0 | **1200 ج** |
| صافي ربح يوليو | −450 ج | **1050 ج** |

### 2. «الذمم المدينة» لا تنقص عند التحصيل — `stats.ts`
كانت تُحسب من فرعي «مبيعات آجل» فقط، بينما السداد يُسجَّل تحت «تحصيل ← تحصيل مبيعات آجلة».

الآن يُطرح التحصيل الفعلي. وحُوِّلت إلى **رصيد تراكمي حتى نهاية الشهر** لأن الذمم بند رصيد
لا بند فترة: بيع آجل في يناير وسداد في فبراير كان سيُظهر ذمماً **سالبة** في فبراير بالحساب الشهري.

بيع آجل 300ج في يوليو ثم تحصيل 100ج في أغسطس ⇒ يوليو **300**، أغسطس **200** (كان: 300 ثم −100).

### 3. قيود كشف حساب العملاء تصير أيتام — `party_ledger`
`party_ledger` لم يكن فيه أي ربط بالبند، فحذف/تعديل فاتورة آجلة كان يترك ديناً وهمياً دائماً.

- migration: عمود `transaction_id` + فهرس.
- **ترحيل غير مُتلِف** للقيود القديمة: تُربط بمطابقة (العميل + المبلغ + الوصف). **لا يُحذف أي قيد
  بالتخمين** — القيد الذي يتعذّر ربطه قد يكون يدوياً أو مُعدَّل الوصف.
- `deleteTransaction` / `deleteShift` يحذفان قيود الكشف المرتبطة.
- `updateTransaction` يُعيد توليد القيد (كان تغيير المبلغ/العميل لا ينعكس إطلاقاً).
- تاريخ القيد = **تاريخ الشيفت** لا تاريخ اليوم (بند مستورد كان يظهر بتاريخ الاستيراد).

### 4·5·6. التوقيت — `core/date.ts` (ملف جديد)
`toISOString()` يُنتج تاريخ **UTC**؛ في مصر (UTC+2/+3) ينزاح يوماً كاملاً.

| الموضع | قبل | بعد |
|---|---|---|
| نهاية شهر أغسطس (لوحة المعلومات) | `2026-08-31` ⇒ **يوم 31 ساقط** | `2026-09-01` |
| `periodEnd` للتقفيل الشهري | `2026-08-30` | `2026-08-31` |
| `todayISO()` الساعة 1:30 ص | `2026-08-17` | `2026-08-18` |

`core/date.ts` يضم `localISO` / `monthEndISO` / `nextMonthStartISO` / `addDaysISO` / `daysInMonth`،
وحسابات الشهر تعمل على النص مباشرةً فهي مأمونة من الانزياح. طُبِّق على لوحة المعلومات والتقارير
و`format.ts` وفي الـ Main Process (`employees.ts`، `treasury.ts`).

### 7. عتبة تنبيه العجز/الأوفر لم تكن تعمل — بل التنبيه كلّه لم يكن يعمل
اتضح أن السبب أعمق ممّا رصده التقرير: `closeShift` — الحاوية الوحيدة لمنطق التنبيه — **لم تكن
تُستدعى من أي شاشة إطلاقاً**. الإقفال الحي يمرّ بـ`updateShiftCloseInputs` ثم `updateShiftStatus`.
فلا تنبيه عجز/أوفر أُنشئ يوماً، رغم وجود شاشة تنبيهات تعرضها وإعداد عتبة يضبطها.

نُقل المنطق إلى مسار الاعتماد الفعلي (`notifyShiftBalance`)، وصار يقرأ العتبة، ويستبدل التنبيه
القديم بدل تكديس نسخ عند إعادة الاعتماد. وحُصِّن حقل العتبة في الإعدادات ضد `NaN`
(إدخال فارغ كان يُخزَّن حرفياً كنص `'NaN'` فتتعطّل القراءة بعدها).

### 8. تسجيل موظف من بوابة السلف كان يفبرك حضوراً
`fillAttendanceForMonth` تملأ الشهر كاملاً بحالة `present` (سلوك مقصود بطلب العميل، أُبقي عليه)
لكن **بلا أي اعتبار لتاريخ التعيين أو ترك العمل** — فتُملأ أيام لم يكن الموظف موظفاً فيها أصلاً
ويُنفَخ `dueSalary`. الآن التعبئة محصورة بين `startDate` و`endDate`.

### 9. الموظف الذي ترك العمل كان يختفي من كشف الرواتب
`getMonthlyFinancials` كانت تقتصر على `status='active'`، فمن استقال يوم 20 يسقط كلياً من كشف
رواتب شهره. الآن يشمل الكشف كل من له حضور أو سلفة فعلية في الشهر ولو أصبح `inactive`.

### 10. الاستيراد كان يصفّر «مبيعات POS» أو «نقدية الكاشير»
الشرط `||` مع تمرير `?? 0`: لو الشيت فيه أحد الحقلين فقط، يُدهس الآخر بصفر — ولو كان المدهوس
مبيعات POS ظهر عجز وهمي بحجم مبيعات الشيفت كله. الآن يُمرَّر الحقل الموجود فقط
(`updateShiftCloseInputs` صار يقبل حقولاً اختيارية).

### 11. الشيفتات المستوردة كانت تبقى بتحصيل ومصروفات = صفر
لأنها لا تمرّ بـ`closeShift` إطلاقاً. أُضيفت `recalcShiftClosingTotals` وتُستدعى دائماً بعد
إدراج بنود الشيفت المستورَد، كما تُستدعى إعادة الاحتساب قبل اعتماد أي شيفت.

### 12. رقم الشيفت الشهري كان يتكرر بعد الحذف
`calcMonthlyShiftNum` كانت `عدد + 1`: شيفتات 1,2,3 ← حذف #2 ← التالي يأخذ **#3 مكرراً**
(ومعه `journal_num` مكرر). صارت `أكبر رقم + 1`.

### 15. حارس القفل الشهري كان مثقوباً
الشهر «المُجمَّد» كان يظل قابلاً للتعديل عبر الخزينة. أُضيف `assertMonthUnlocked` إلى
`addTreasuryAdjustment` و`addTreasuryCheckpoint` و`savePayrollReport` و`deletePayrollReport`.

### 17. «إعادة الضبط الكاملة» كانت تُبقي نقاط ارتكاز الخزينة
`treasury_checkpoints` (و`party_ledger`) لم يكونا في قائمة المحو، فيبقى بعد «محو كل شيء»
رصيد صندوق افتتاحي موروث من بيانات ممحوّة يُفسد كل حسابات الخزينة اللاحقة. أُضيفا.

### إضافي — خصم مزدوج في وضع «كل الفترات» بشاشة الصندوق
`dateLike` في وضع `all` هو `'%'`، فكانت التسويات المؤرَّخة **قبل أول شيفت** تُحتسَب مرتين:
داخل `prevBalance` وداخل حركات الفترة. أُضيف شرط `date >= monthStart`.

### إضافي — بيانات اعتماد قديمة تبقى بعد إعادة فتح الشيفت
`updateShiftStatus` لغير `approved` كانت تحدّث الحالة فقط، فيظل الشيفت المفتوح يحمل
`approved_by`/`approved_at`. صارت تُصفَّر.

### إضافي — عمليات مركّبة بلا معاملة
`createShift` (شيفت + يومية + فوري + عهدة) و`deleteShift` (7 عمليات حذف) صارتا داخل
معاملة واحدة — أي فشل جزئي كان يترك شيفتاً بلا يومية فتفشل عليه كل إضافات البنود لاحقاً.

### إضافي — حذف شيفت في شهر مُقفَل كان يفشل بصمت
`catch (e) { console.error(e) }` كان يبتلع رسالة حارس القفل الشهري، وهي بالضبط ما يحتاج
المستخدم قراءته. صارت تظهر في Toast. واستُبدل `alert()` الأصلي في نفس الدالة بـToast
(مخالفة صريحة لـ`CLAUDE.md`: حوار النظام الحاجز يعطّل تركيز نافذة Electron فيُجمّد
أول `<input type="date">` بعده — وشاشة التقارير مليئة بها).

---

## ثانياً: الكود الميت (حُذف)

| ما حُذف | الحجم | لماذا كان ميتاً |
|---|---|---|
| `getOverview` + `OverviewData` + `Trend` + `prevMonth` + `pctTrend` | ~85 سطراً | غير مربوطة بأي قناة IPC |
| `closeShift` + قناة `shifts:close` + مدخل preload | ~60 سطراً | لا تُستدعى من أي شاشة (منطق التنبيه بداخلها نُقل، لم يُحذف) |
| شارة الإشعارات في الشريط الجانبي + `setInterval` كل 30 ث | ~30 سطراً | تقارن بـ`'notifications'` وهي قيمة غير موجودة في نوع `Page` ولا في `NAV` — `tsc` كان يرصدها (`TS2367`) |
| المعامل الخامس `_cashierRemaining_ignored` | — | معلَّم صراحةً «لم يعد مستخدماً» |

`stats.ts`: 146 → 94 سطراً. خطأ `TS2367` في `Sidebar.tsx` اختفى.

---

## ثالثاً: ما لم يُنفَّذ عمداً

| البند | السبب |
|---|---|
| نظام الترخيص (5 مشاكل) | `CLAUDE.md` يمنع المساس به بلا تأكيد صريح |
| فحص الصلاحيات في الـ Main Process (#13) | ثغرة أمنية لا خطأ محاسبي، وتحتاج قراراً بشأن سلوك كل قناة |
| كتابة أنواع طبقة الـ IPC (#27) | 15 خطأ `tsc` قائم في `store/shift.ts` و`Categories.tsx` — تغيير واسع خارج نطاق الطلب |
| حذف القيود اليتيمة القائمة | الترحيل يربط ما يمكن ربطه؛ حذف قيد محاسبي بالتخمين غير مقبول |
| «مرتجع مشتريات» و«المسحوبات الشخصية» ضمن التكاليف | نموذج محاسبي قائم وغامض — تغييره قرار للمالك لا تصحيح خطأ |
| نسخ `before-restore` بلا سقف (#16) · استيراد `src/` من `electron/` (#28) · N+1 (#35) | ليست أخطاء محاسبية ولا كوداً ميتاً |
