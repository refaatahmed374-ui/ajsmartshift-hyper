# ملاحظات مشروع — AJ Smart Shift Hyper

هذا الملف مرجع سريع لقرارات مهمة تم التوصل لها مع المطوّر (أحمد جلال) ويصعب استنتاجها من قراءة
الكود وحدها. اقرأه أول أي جلسة عمل على هذا المستودع. للتفاصيل الكاملة لكل تعديل راجع `CHANGELOG.md`.

## نظام الترخيص — مهم جداً، لا تلمسه بدون تأكيد صريح

- التطبيق **يستخدم حالياً نظام الترخيص المباشر عبر Firestore** (الخطة المجانية Spark — بلا اشتراك
  مدفوع). الكود في `electron/database/repositories/license.ts` يتصل مباشرة بـ Firestore REST API.
- لوحة التحكم الحقيقية اللي بيستخدمها أحمد جلال: `https://ajsmartshift.web.app/admin` — بتكتب
  مباشرة في Firestore (`licenses/{machineId}`), مش عن طريق أي API وسيط.
- **يوجد مشروع منفصل** (`AJPOS PRO Licensing Service` + مستودعات شقيقة تحت نفس مجلد
  `01 - Source Code`) بيطوّر نظام ترخيص موحّد جديد (Cloud Functions + توقيع RSA) لكل إصدارات AJPOS
  PRO الأربعة. **هذا النظام متوقف عمداً ولم يُنشر أبداً** — القرار الصريح كان تأجيله لحد ما يخلص
  تطوير Hyper POS. لو لقيت كود بيشاور على `ajpos-pro-licensing-client` أو مفاتيح RSA، ده الأثر
  المتبقي من محاولة سابقة تم التراجع عنها بالكامل — **لا تعيد تفعيله بدون تأكيد صريح من المستخدم**.

## معادلة التقفيل (عجز/أوفر) — core/engine.ts → calcShiftClosing

المعادلة الرسمية الموحّدة لكل الشاشات (الشاشة الحيّة، التقارير، لوحة المعلومات، PDF):

```
النتيجة = (نقدية الكاشير + مصروفات الكاشير − التحصيل) − (مبيعات POS + مبيعات فوري)
موجب ⇒ أوفر · سالب ⇒ عجز · صفر ⇒ مطابق
```

نقاط حسّاسة تم التأكد منها بالتجربة الفعلية (شيت "حورس" المرجعي + أمثلة رقمية من المستخدم)، **لا
تُغيَّر بدون دليل مماثل**:

- **مبيعات فوري تدخل المعادلة** — فلوسها فعليًا في درج الكاشير. استُخدِمت `fawryTotalManual` فقط
  (الإدخال اليدوي الحي)، **وليس** `programSales` (القيمة المستورَدة من إكسل).
- **سبب استبعاد `programSales` من المعادلة تحديدًا**: للشيفتات المستورَدة من إكسل، عمود "اجمالي
  مبيعات" (`posSales`) في الشيت الأصلي كان أصلاً شاملاً "مبيعات البرنامج" — فإضافتها تاني في
  المعادلة تُحتسَب مرتين. هذا الاستثناء خاص بحالة الاستيراد فقط.
- خلية "اجمالي المبيعات" المعروضة في ملخّص الشيفت (`posSales + fawryWithCommission` مع رجوع
  لـ`programSales` للاستيراد) هي **قيمة عرض/تقرير فقط** ومنفصلة عمداً عن معادلة التقفيل — لا تحاول
  توحيدهما.

## سياسة النسخ الاحتياطي

- `electron/database/repositories/backups.ts` — `createBackup`/`restoreBackup` **غير متزامنة عمداً**
  (كانت متزامنة وتسبب تجميد كامل للـ Main Process أثناء ضغط قاعدة بيانات كبيرة — أي IPC، بما فيه فتح
  شيفت جديد، كان يتجمد لحد ما تخلص).
- سقف **5 نسخ غير يدوية بحد أقصى**، مجموع واحد (يومي + عند الخروج + قبل استيراد إكسيل) — مش لكل نوع
  سقف منفصل. النسخ اليدوية (زرار "نسخة احتياطية الآن") مُستثناة دائماً من العدّ والحذف التلقائي.

## لا تستخدم `confirm()`/`alert()` الأصلية أبداً

اكتُشف إن حوارات النظام الحاجزة (`window.confirm`/`window.alert`) بتعطّل تركيز نافذة Electron بشكل
بيخلّي أول عنصر واجهة يعتمد على widget نظام أصلي بعدها (زي `<input type="date">`) يفضل غير مستجيب
لثوانٍ. استُبدلت كل الاستخدامات بمكوّن `src/components/ConfirmDialog.tsx` (أو الأنماط المضمَّنة
المشابهة في `ShiftSheet.tsx`). **أي تأكيد جديد لازم يستخدم نفس النمط، مش `confirm()`/`alert()`
مباشرة.**

## بنية مشتركة

- `core/` هو المجلد المشترك بين `electron/` (Main process) و`src/` (Renderer) — أي منطق يُستخدَم في
  الجانبين (حسابات، تطبيع نصوص، تشابه نصوص) لازم يعيش هنا، مش داخل `electron/` فقط، وإلا `tsc` على
  `tsconfig.web.json` هيرفضه (بيقتصر على `src/**` و`core/**` فقط).

## النشر

راجع `README.md` (قسم "البناء والنشر") — `git tag vX.Y.Z && git push origin vX.Y.Z` كافي لتشغيل بناء
ونشر تلقائي كامل عبر GitHub Actions. المستودع خاص، والإصدارات المبنية تُنشر تلقائيًا في مستودع عام
منفصل (`refaatahmed374-ui/ajsmartshift-releases`) يقرأ منه تحديث التطبيق التلقائي عند العملاء.
